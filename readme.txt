Problema
Cuando despliegas tu aplicación localmente, el frontend y el backend pueden comunicarse sin problemas,
pero al desplegar en Render, esta comunicación falla. Esto podría deberse a varios factores como la configuración

en scrip.js cambiamos el localhost por el back con direccion https://backend-t4xr.onrender.com:5000

Una vez ejecutado el compose se debe crear la base docker-compose exec backend python migrate.py 
